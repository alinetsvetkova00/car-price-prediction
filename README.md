The trained model file (car_price_model.pkl) is not included in the repository due to its large size. It can be recreated by running the notebook notebooks/analysis.ipynb.

You can check the link too: http://192.168.0.27:8501
